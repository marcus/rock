# Deployment configuration for Roy's Rock Machine
# This file should be gitignored and not committed to the repository

# Server details (using the same server as in deploy.md)
SERVER_IP="146.190.117.215"
SERVER_USER="marcus"
SERVER_DIR="/home/marcus/roysrock"
DOMAIN_NAME="beats.opentangle.com"

# Docker registry
DOCKER_REGISTRY="ghcr.io"
DOCKER_USERNAME="marcus"
DOCKER_IMAGE="roysrock"
DOCKER_TAG="latest"

# Port configuration (matches nginx config in deploy.md)
PORT="5060"